---
const { kicker, title, copy } = Astro.props;
---

<div class="section-head reveal"><div><span class="section-kicker">{kicker}</span><h2 class="section-title">{title}</h2></div><div class="section-copy">{copy}</div></div>
