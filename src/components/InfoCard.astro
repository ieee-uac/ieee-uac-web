---
// `icon` es opcional: en la portada las tarjetas no llevan letra, en Acerca de sí.
const { icon, title, text } = Astro.props;
---

<article class="info-card reveal">{icon && <span class="icon-letter">{icon}</span>}<h3>{title}</h3><p>{text}</p></article>
